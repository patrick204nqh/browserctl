pre-commit:
  commands:
    rubocop:
      glob: "{**/*.rb,Rakefile,Brewfile}"
      run: bundle exec rubocop --autocorrect-all {staged_files}
    rbs-validate:
      glob: "**/*.rbs"
      run: bundle exec rbs validate

pre-push:
  commands:
    rspec:
      run: bundle exec rspec
